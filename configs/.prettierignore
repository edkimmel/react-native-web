../coverage
../**/dist
../flow-typed
../node_modules
../packages/**/vendor/*
../packages/**/.next
# Scratch reproductions live at the repo root and are not part of the
# package sources. They are untracked working files (and `repro/*/.next`
# is build output), so formatting them is meaningless — but `npm test`
# runs `prettier --check "**/*.js"` from the repo root, so without this
# the project's own entry-point command fails on files nobody edits.
../repro
